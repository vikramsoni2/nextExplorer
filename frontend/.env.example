# With Vite proxy, leave VITE_API_URL unset in dev
# Optional: comma-separated list of file extensions (without dots) that should open in the editor
VITE_EDITOR_EXTENSIONS=txt,md,csv,json,js,ts,py
\n+# Optional: override app version shown in Settings → About
# VITE_APP_VERSION=1.2.3
