# With Vite proxy, leave VITE_API_URL unset in dev
# Optional: comma-separated list of file extensions (without dots) that should open in the editor
VITE_EDITOR_EXTENSIONS=txt,md,markdown,csv,tsv,json,json5,yml,yaml,xml,log,ini,cfg,conf,env,properties,js,jsx,mjs,cjs,ts,tsx,py,rb,php,java,c,h,cpp,hpp,cs,go,rs,swift,kt,scala,sh,bash,zsh,ps1,bat,vue,svelte,astro,html,css,scss,sass,less
# Optional: override app version shown in Settings → About
# VITE_APP_VERSION=1.2.3

# Optional: embed git info in About (for local builds)
# VITE_GIT_COMMIT=deadbeefdeadbeefdeadbeefdeadbeefdeadbeef
# VITE_GIT_BRANCH=feature/my-branch
# VITE_REPO_URL=https://github.com/owner/repo
