services:
  app:
    
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - /Users/vikram/projects:/mnt/Projects
      - /Users/vikram/Downloads:/mnt/Downloads
      - /Users/vikram/Downloads/config:/config
      - /Users/vikram/Downloads/cache:/cache
    environment:
      - LOG_LEVEL=debug
      - DEBUG=openid-client*

      # Match these to your host user/group IDs (check with `id -u` / `id -g`)
      - PUID=1000
      - PGID=1000
      # Centralized public URL (frontend + backend behind proxy)
      - PUBLIC_URL=https://files.vsoni.com
      
      
      # OIDC configuration (fill with your identity provider details)
      - OIDC_ENABLED=true
      - OIDC_ISSUER=https://auth.vsoni.com/application/o/next/
      - OIDC_CLIENT_ID=6HBh3lMneUwWCAeutdEgwNZ6myXzFc93clojdfmQ
      - OIDC_CLIENT_SECRET=w2ACQfUhhk2vZ14m2GVvYbWqkCQTiXyMsowpLQE2JtiPfaCTrBKhZHwZACgHOCWgSs9zhJW0j8y1TYCrLs0hsMmM0xdIz5tRmce6o4ymVOCQByPRIr9xMxtBt1Spg0I1
      - OIDC_SCOPES=openid,profile,email,groups
      - OIDC_ADMIN_GROUPS=next-admin,admins

      - ONLYOFFICE_URL=https://office.vsoni.com
      - ONLYOFFICE_SECRET=ha96M546Hl5oSSvrjvabmXK1D6qlRH15
      - ONLYOFFICE_LANG=en
      - ONLYOFFICE_FILE_EXTENSIONS=doc,docx,xls,xlsx,ppt,pptx,odt,ods,odp,rtf,txt,pdf

      # Authentication configuration
      # AUTH_MODE: Controls which authentication methods are available
      #   - 'local': Only username/password authentication
      #   - 'oidc': Only OIDC/SSO authentication
      #   - 'both': Both local and OIDC (default if not specified)
      #   - 'disabled': Skip authentication entirely (same as AUTH_ENABLED=false)
      # - AUTH_MODE=disabled

      # Legacy auth toggle (deprecated, use AUTH_MODE=disabled instead)
      # - AUTH_ENABLED=false
      
      # Optional: bootstrap the first local admin (skips setup wizard)
      # - AUTH_ADMIN_EMAIL=admin@example.com
      # - AUTH_ADMIN_PASSWORD=please-change-me

    restart: unless-stopped
