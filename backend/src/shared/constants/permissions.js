/**
 * Permission Constants
 * Access control permission levels
 */

const PERMISSIONS = {
  READ_WRITE: 'rw',
  READ_ONLY: 'ro',
  HIDDEN: 'hidden'
};

const ROLES = {
  ADMIN: 'admin',
  USER: 'user'
};

module.exports = {
  PERMISSIONS,
  ROLES
};
